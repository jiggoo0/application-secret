/** @format */

/**
 * 🎛️ DESIGN TOKENS — Industrial Sharp V4.0 (Production)
 *
 * แนวคิดการออกแบบ
 * 1) เน้นข้อมูลเป็นหลัก ภาพและสีต้องไม่รบกวนการอ่าน
 * 2) คอนทราสต์สูง อ่านชัดบนพื้นหลังสีเข้ม
 * 3) ใช้สีแบรนด์เฉพาะจุดสำคัญ เพื่อสื่อความน่าเชื่อถือและอำนาจแบรนด์
 *
 * หมายเหตุสำหรับ AI / ระบบอื่น:
 * - ไฟล์นี้เป็นแหล่งอ้างอิงกลางด้านสี ฟอนต์ และ interaction
 * - ห้ามแก้ค่าโดยไม่ผ่านการพิจารณาเชิงระบบ
 */

export const DESIGN_TOKENS = {
  /* ------------------------------------------------------------------
   * COLOR SYSTEM
   * ------------------------------------------------------------------ */
  colors: {
    // สีแบรนด์หลัก
    brand: '#FCDE09',
    brandDark: '#D4BC08',

    // พื้นหลังและตัวอักษรหลัก
    background: '#020617', // พื้นหลังหลักของระบบ
    textPrimary: '#F1F5F9', // เนื้อหาหลัก
    textSecondary: '#CBD5E1', // ข้อความรอง / label
    textMuted: '#64748B', // ข้อความความสำคัญต่ำ

    // สถานะ
    white: '#FFFFFF',
    success: '#10B981',
    error: '#EF4444',
  },

  /* ------------------------------------------------------------------
   * TYPOGRAPHY
   * ------------------------------------------------------------------ */
  typography: {
    // หัวเรื่องหลัก ใช้ใน Hero / Section สำคัญ
    h1: 'text-6xl md:text-8xl lg:text-[110px] font-black uppercase italic tracking-tighter leading-[0.85] text-white',

    // หัวเรื่องรอง
    h2: 'text-4xl md:text-6xl font-black uppercase tracking-tighter leading-none text-white',

    // ป้ายข้อมูล เช่น ID, Log, Reference
    label: 'font-mono text-[10px] font-black uppercase tracking-[0.4em] text-slate-300',

    // เนื้อหาทั่วไป อ่านง่าย ชัดเจน
    body: 'font-thai text-base md:text-lg font-bold leading-relaxed text-slate-200',
  },

  /* ------------------------------------------------------------------
   * SHADOW SYSTEM
   * ------------------------------------------------------------------ */
  shadows: {
    // เงาแนวอุตสาหกรรม คม แข็ง ไม่เบลอ
    sharp: 'shadow-[6px_6px_0px_0px_#020617]',
    sharpBrand: 'shadow-[6px_6px_0px_0px_#FCDE09]',
    sharpWhite: 'shadow-[6px_6px_0px_0px_#FFFFFF]',
  },

  /* ------------------------------------------------------------------
   * INTERACTION
   * ------------------------------------------------------------------ */
  interaction: {
    // การเคลื่อนไหวหลักของระบบ
    transition: 'transition-all duration-300 cubic-bezier(0.16, 1, 0.3, 1)',

    // เอฟเฟกต์ hover มาตรฐาน
    hover: 'hover:-translate-x-1 hover:-translate-y-1',
  },
} as const

/* ------------------------------------------------------------------
 * TYPE EXPORT
 * ------------------------------------------------------------------ */
export type DesignTokens = typeof DESIGN_TOKENS
