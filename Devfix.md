🔧 AI Context: Dev Fix (JP Visual Docs)

🎯 วัตถุประสงค์

- ใช้เป็น มาตรฐานการแก้ไขโค้ด และโครงสร้างโปรเจกต์
- เน้น ความแม่นยำสูง → ทุกการแก้ไขต้องสอดคล้องกับ Protocol และ Project Structure ที่กำหนด
- ห้ามผสมตัวแปรหรือ logic ที่ไม่เกี่ยวข้อง

---

🗂 ขอบเขตการแก้ไข

1. โครงสร้างไฟล์ (Project Structure)
   - ต้องตรวจสอบว่าไฟล์อยู่ในตำแหน่งที่ถูกต้องตาม Tree ที่กำหนด
   - ห้ามสร้างโฟลเดอร์หรือไฟล์ใหม่ที่ไม่อยู่ใน Context
   - หากพบโฟลเดอร์พิเศษ เช่น .Z → ห้ามนำมาผสม

2. โค้ด (Code Standards)
   - Type Safety → ต้องใช้ TypeScript แบบ Strict Mode, ห้ามใช้ any
   - Clean Code → แยก Client/Server Component ชัดเจน, ห้ามเขียนไฟล์ยาวเกิน 200–300 บรรทัด
   - Single Source of Truth → ดึงค่าจาก config/ (site.ts, theme.ts, navigation.ts) เสมอ
   - Styling → ใช้ Tailwind CSS แบบ Mobile First, Theme Industrial Sharp (Slate, Grid Lines, Blueprint Typography)
   - Semantic HTML → ใช้ <section>, <article>, <nav> แทน <div> ฟุ่มเฟือย

3. UI/UX (Visual & Interaction)
   - ต้องสื่อถึงความเป็นระบบและน่าเชื่อถือ (Industrial Sharp)
   - ห้ามใช้สีฉูดฉาดหรือกราฟิกตลาดล่าง
   - ใช้ next/image สำหรับรูปภาพ, ระบุ priority สำหรับ Hero

---

⚠️ ข้อควรระวัง

- ห้าม Hard-coded Values (เช่น Hex Code, ข้อความสำคัญ) → ต้องดึงจาก Config
- ห้าม Import แบบสะเปะสะปะ → ต้องใช้ @/ alias
- ห้ามเขียน Logic ที่ไม่รองรับจริง (Fake Logic)
- ห้ามผสมโครงสร้างพิเศษ .Z หรือข้อมูลนอก Context

---

✅ วิธีใช้ Context นี้

- เมื่อแก้ไขโค้ด → ตรวจสอบว่าโครงสร้างและมาตรฐานตรงกับ Protocol
- เมื่อเพิ่ม Component → ต้องอยู่ในตำแหน่งที่กำหนด เช่น components/section/ หรือ components/ui/
- เมื่อแก้ UI → ต้องคุมโทน Industrial Sharp และใช้ค่าจาก theme.ts

---

💡 ตัวอย่างคำสั่ง Dev Fix

- “จาก AI Context Dev Fix ช่วยแก้ไข ServiceCard.tsx ให้รองรับการใช้สีจาก theme.ts และปรับโครงสร้างให้เป็น Mobile First โดยไม่ใช้ Hard-coded Hex Code”
- “จาก AI Context Dev Fix ตรวจสอบ HeroSection.tsx ว่าใช้ <section> และ next/image ตามมาตรฐานหรือไม่ ถ้าไม่ให้แก้ไขให้ถูกต้อง”
